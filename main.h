#ifndef MAIN_H
#define MAIN_H (main)
typedef struct datatype
{
    char *letra;
    int (*funcion)(char *arg);
} datatype;
int _putchar(char c);
int _printf(const char *format, ...);
#endif /* DOG_H */
